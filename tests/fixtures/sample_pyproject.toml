[project]
name = "sampleproject"
version = "0.1.0"
dependencies = [
    "torch>=2.0",
    "transformers>=4.30",
    "fastapi>=0.100",
    "uvicorn>=0.20",
    "scikit-learn>=1.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "mypy>=1.0",
]
